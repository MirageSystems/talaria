name: ci

on:
  push:
  pull_request:

jobs:
  repository:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check repository metadata
        run: |
          test -f README.md
          test -f LICENSE
          test -f .gitignore
